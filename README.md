# PyDependenciesUpdater
Updates all your Dependencies on execution and checks if you have the newest Python version

## Setup

`pip3 install requests`
`pip3 install termcolor`
`pip3 install bs4`

## Running

* Execute PyUpdater.py to automatically update all of your Dependencies
